# wolo

Populate /etc/ethers (see `man ethers`) and run with:

```
wolo --bind 0.0.0.0:3000
```

This sets up a web interface that lets you easily broadcast Wake-On-Lan magic
packets.

![showcase](showcase.png)
